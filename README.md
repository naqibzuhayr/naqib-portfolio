# Naqib Portfolio

Developer portfolio built with Next.js, TypeScript, App Router, and Tailwind CSS.

```bash
npm install
npm run dev
```

Content lives in `src/data`; reusable UI lives in `src/components`.
